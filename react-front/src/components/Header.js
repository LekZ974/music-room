import React from 'react';

import { Header as NBHeader } from 'native-base';

const Header = props => {
  return <NBHeader style={{ backgroundColor: 'blue' }} {...props} />;
};
export default Header;
